# Application Environment: 'dev', 'test', or 'prod'
APP_ENV=dev

# A secret key for signing sessions, JWTs, etc. Generate with: openssl rand -hex 32
SECRET_KEY=

# Database connection string
# For Docker Compose (recommended): postgresql+psycopg2://smbuser:smbpass@db:5432/smbsec
# For Local Development: postgresql+psycopg2://user:pass@localhost:5432/smbsec
DB_URL=postgresql+psycopg2://smbuser:smbpass@db:5432/smbsec

# Email settings for alerts (optional)
SMTP_HOST=
SMTP_USER=
SMTP_PASS=

# OSINT API Keys (add as needed)
OSINT_SHODAN_API_KEY=
OSINT_CENSYS_API_ID=
OSINT_CENSYS_API_SECRET=
OSINT_OTX_API_KEY=

# Base URL for the backend
BASE_URL=http://localhost:8000